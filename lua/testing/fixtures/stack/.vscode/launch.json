{
  "version": "0.2.0",
  "configurations": [
    {
      "name": "Stack",
      "type": "pwa-node",
      "request": "launch",
      "program": "${workspaceFolder}/deep.js",
      "cwd": "${workspaceFolder}"
    }
  ]
}