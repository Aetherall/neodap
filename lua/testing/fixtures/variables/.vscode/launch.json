{
  "version": "0.2.0",
  "configurations": [
    {
      "name": "Variables",
      "type": "pwa-node",
      "request": "launch",
      "program": "${workspaceFolder}/complex.js",
      "cwd": "${workspaceFolder}"
    },
    {
      "name": "Deep Nested",
      "type": "pwa-node",
      "request": "launch",
      "program": "${workspaceFolder}/deep_nested.js",
      "cwd": "${workspaceFolder}"
    }
  ]
}