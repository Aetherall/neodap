{
  "version": "0.2.0",
  "configurations": [
    {
      "name": "Variables",
      "type": "pwa-node",
      "request": "launch",
      "program": "${workspaceFolder}/complex.js",
      "cwd": "${workspaceFolder}"
    }
  ]
}