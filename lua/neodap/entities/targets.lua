-- Targets entity methods for neograph-native
-- UI grouping node for leaf sessions (debug targets)
return function(Targets)
  ---Get display label for this node
  ---@return string
  function Targets:label()
    return "Targets"
  end
end
